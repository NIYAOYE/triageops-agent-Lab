# Assets

Place the homepage screenshot here:

```text
assets/triageops-workbench.png
```

Recommended capture size: 1440x900 or wider, PNG format. The root `README.md` already references this path, so replacing or adding that file will make the image render on GitHub.
